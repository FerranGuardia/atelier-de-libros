# Atelier de Libros

Boutique literary styling agency — editorial site, Astro + TS, vanilla CSS, no backend.

The brief: [briefing-web.md](briefing-web.md). The rules: [CLAUDE.md](CLAUDE.md). The workflow: [.claude/README.md](.claude/README.md).

`npm run dev` to start. `npm run build` to build.
