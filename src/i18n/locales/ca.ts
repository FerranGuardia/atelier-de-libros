/* T04b — Catalan translation. Currently null everywhere except the
 * smoke-test key (`nav.home`) so the resolver can be verified end-to-end.
 * Awaiting Mireia. */

import type { PartialDictionary } from '../types'

const dict: PartialDictionary = {
  nav: {
    home: 'Atelier'
  }
}

export default dict
