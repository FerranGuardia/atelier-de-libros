/* T04b — French translation. Currently null everywhere except the
 * smoke-test key (`nav.home`). Awaiting translator. */

import type { PartialDictionary } from '../types'

const dict: PartialDictionary = {
  nav: {
    home: 'Atelier'
  }
}

export default dict
