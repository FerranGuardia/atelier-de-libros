/* T04b — Italian translation. Currently null everywhere except the
 * smoke-test key. */

import type { PartialDictionary } from '../types'

const dict: PartialDictionary = {
  nav: {
    home: 'Atelier'
  }
}

export default dict
