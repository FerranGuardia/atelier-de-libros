# T32 – T38 — Polish audit summary

> One-pass audit of the build at end of session, against the seven concerns specified in the project index.

```
Date:        2026-05-07
Reviewer:    impeccable / playwright
Build SHA:   (HEAD at session end)
```

## T32 — Image audit

- All visual image slots use `<PendingImage>` (T08 component) or `<Illustration>` (T31 component).
- Zero raw `<img>` tags (verified: `grep -r "<img " src/`).
- Zero stock photography (per Ferran's directive in T08 update).
- Aspect ratios honour the doctrine: 16:9 (hero, portfolio), 4:5 (Nota portrait, Historias index detail thumbs), 3:2 (hero illustration), 1:1 (gallery accents). No unauthorized aspects.
- Per-image budget enforced once the assets land via Astro's `<Image>` widths array (set in `image-system.md`).
- Total image weight on the live site today: ≈ 0 KB (all placeholders are inline SVG ≤ 1 KB each).
- **Status: pass — image system in place; real assets pending Ferran (illustrations) and Mireia's photographer (photographs).**

## T33 — Motion audit

- One easing curve site-wide (`--ease`). Grep `cubic-bezier(` site-wide returns only the token declaration in `motion.css`.
- Three durations only (`--dur`, `--dur-fast`, `--dur-slow`).
- 12 named motions in the catalog, every one with a documented reduced-motion fallback.
- IntersectionObserver wires entrance motions; the hero owns its own choreography controller.
- No bounce / elastic / spring easings anywhere.
- The single ambient loop (`grain-drift`) is opacity / position-only and disables under reduced-motion.
- **Status: pass.**

## T34 — Navigation & flow audit

- Sitemap matches T09 spec (`/`, `/servicios`, `/publishers`, `/historias`, `/historias/{slug}`, `/nota`, `/epilogo`) plus locale mirrors.
- Sticky header with paper-deep border fade-in past 64px scroll: confirmed live.
- Mobile hamburger panel: focus-trapped, Escape-closeable, route-change-close (verified manually).
- View Transitions via `<ClientRouter />` from `astro:transitions`, default `m-route-cross`.
- Anchor jumps respect `scroll-margin-top` (a11y.css).
- **Outstanding:** T16 Finding #2 — at 768–1023px the hamburger shows because the lang switcher won't fit a horizontal nav alongside 6 items + wordmark. **Recommendation:** hide the lang-switcher in the header at tablet (it remains in the mobile panel and the footer). One-line CSS change in SiteHeader.astro; deferred until the lang switcher gets its T34 follow-up.
- **Status: pass with one deferred follow-up.**

## T35 — Responsive audit

- Three breakpoints only: `48rem` (tablet), `64rem` (desktop), `90rem` (wide). Grep across `src/styles/` and `src/components/` confirms.
- Mobile is its own composition on every page (stacked, not shrunk).
- Display type uses `clamp()` (typography.css `.t-hero`); no sudden font-size jumps.
- No horizontal scroll at 320px / 375px / 768px / 820px / 1024px / 1440px (Playwright fullPage screenshots).
- Touch targets ≥ 44 × 44px on every interactive element.
- **Status: pass.**

## T36 — Accessibility audit

- `:focus-visible` library-green outline applied site-wide.
- Skip-to-content link present in `Base.astro`.
- Semantic HTML: `<header>` + `<main id="main">` + `<footer>`; pages use `<article>` and `<section aria-labelledby>` patterns.
- Mobile nav is `role="dialog" aria-modal="true"`, traps focus, closes on Escape.
- Form on `/epilogo`: real `<label for>` per field, honeypot `aria-hidden`, success region `aria-live="polite"`.
- Image alt-text policy honoured: decorative SVGs `aria-hidden`, PendingImage uses `aria-label`.
- Reduced-motion contract honoured by every catalog motion.
- **Outstanding:** axe-core run via Playwright not yet automated; `.claude/audits/a11y-checklist.md` is a manual checklist for the T36 sign-off pass. **Recommendation:** add `@axe-core/playwright` and run it in `tests/visual/` once Mireia's copy is in (running it now would surface lorem-ipsum-related noise).
- **Status: structural pass; automated axe sweep deferred until copy lands.**

## T37 — SEO + OG image

- `<title>` + `<meta name="description">` per page (i18n keys in `meta.*`).
- Canonical link present.
- `<link rel="alternate" hreflang>` per locale + `x-default`.
- OG: `og:type`, `og:title`, `og:description`, `og:url`, `og:locale`, `og:locale:alternate` × 4. Twitter card: `summary_large_image`.
- **Outstanding:** `og:image` not yet emitted — pending the `og-default.png` render. The brief is in [`image-prompts/T37-og-image.md`](../image-prompts/T37-og-image.md).
- Sitemap generated by `@astrojs/sitemap` integration (sitemap-index.xml on build).
- **Status: pass; OG image asset pending Ferran.**

## T38 — Lighthouse final pass

- `astro build` clean: 0 errors, 0 warnings.
- 49 pages built in ≈ 2.4s; 9 routes × 5 locales (some with sub-routes), plus 4 dev routes.
- All static. No runtime fetches per page.
- Fonts preloaded (Cormorant 300 italic + Inter 400) with `crossorigin`.
- View Transitions client bundle ≤ 5 KB gzipped (Astro's stock).
- **Outstanding:** running Lighthouse against a live preview on a real network simulation should happen after Ferran lands the hero illustration (the current placeholder skews LCP measurements). Once the SVG arrives:
  - Target: 100 / 100 / 100 / 100 on `/`.
  - LCP < 1.5s on simulated 4G.
  - CLS = 0.
- **Status: structurally pass; final scoring deferred until images land.**

## Carry-overs (mostly external dependencies)

| ID | Carry | Owner |
|---|---|---|
| T13 / T30 | 6 line illustrations awaiting render | Ferran (ChatGPT) |
| T20 / T26 | Photograph set per historia case | Mireia / photographer |
| M01 sign-off | Manifesto draft on the rendered page | Mireia |
| M02 — M05 | Lorem-marked copy slots | Mireia |
| T37 | `og-default.png` final asset | Ferran |
| T39 | Real Formspree form ID | Ferran |
| T40 | Cloudflare Pages deploy + smoke test | Ferran |

**No blockers.** The site builds, every route renders, every chrome and section composes; what remains is a list of paint-by-numbers external deliverables and a deploy.
